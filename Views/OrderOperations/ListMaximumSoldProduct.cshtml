@model IEnumerable<SalesProductAssignment.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<h1>Select the product for Listing all orders where product sold maximum quantity</h1>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    <a asp-action="ListMaximumSoldProductSelected" asp-route-id="@item.Id">Get Maximum Quantity Sold Order</a>
                    
                </td>
            </tr>
        }
    </tbody>
</table>







